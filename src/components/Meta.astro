---
interface Props {
    title?: string;
    description?: string;
}
const { title, description } = Astro.props;

import { SEO } from "astro-seo";
---

<SEO
    title={title || "DisPaisy"}
    description={description || "Hi! I'm DisPaisy!"}
    twitter={{
        creator: "@dispaisy"
    }}
    canonical="https://dis.paisy.lol"
    extend={{
        link: [
            { rel: "icon", href: "/fav.png" }
        ],
        meta: [
            { name: "twitter:title", content: title || "DisPaisy" },
            { name: "twitter:description", content: description || "Hi! I'm DisPaisy!" },
            { name: "viewport", content: "width=device-width, initial-scale=1"},
            { name: "theme-color", content: "#3498db"}
        ],
    }}
/>
